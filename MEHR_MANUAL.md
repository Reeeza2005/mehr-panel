# شناسنامه و دفترچه راهنمای جامع پروژه «مِهر» (Mehr Panel)
> این سند مرجع دائمی همکاری، قوانین رفتاری، معماری فنی و دستاوردهای پروژه مِهر است.

---

## ۱. قوانین طلایی همکاری (غیرقابل تغییر)
1. **قانون بدون حدس (Zero Guesswork):** هیچ کدی حدس زده نمی‌شود، بدون اجازه تغییری اعمال نمی‌شود و پیش از هر تصمیم فنی ابتدا توضیح شفاف داده شده و اجازه گرفته می‌شود.
2. **یک گام در هر مرحله:** تا وقتی یک تغییر یا فیچر به طور ۱۰۰٪ تست نشده و پینگ و خروجی سالم نداده، به مرحله بعد ورود نمی‌کنیم.
3. **پشتیبان‌گیری اجباری:** پیش از هرگونه تغییر در فایل‌های اصلی، حتماً یک نسخه بکاپ زمان‌دار گرفته می‌شود.
4. **ثبت تجربه:** هر بخش موفقی که راه‌اندازی شد، فوراً ساختار و نحوه کار آن در این فایل مستند می‌شود تا در آینده فراموش نشود.

---

## ۲. هویت بصری و برندینگ (مِهر)
- **نام سامانه:** مِهر (Mehr Panel) - هرگز از نام پیش‌فرض نهان در پنل، سابسکریپشن و خروجی‌ها استفاده نمی‌شود.
- **تایتل اشتراک:** پروفایل‌ها با عنوان اختصاصی «مِهر» برای کلاینت‌ها صادر می‌شوند.
- **روتر هوشمند مِهر:** مکانیزم هوشمند اختصاصی مِهر برای تفکیک ترافیک و مسیرهای اتصال.

---

## ۳. معماری و دستاوردهای تثبیت‌شده (تجربیات موفق)
- **لینک کاربر و سابسکریپشن:** ساختار اختصاصی برای استخراج سابسکریپشن کاربران به شکل `/sub/:username` که نباید خراب شود.
- **فرمول پینگ سالم:** 
  - تنظیم آدرس اتصال روی Clean IP کلودفلر.
  - تنظیم Host و SNI روی ورکر/دامنه هدف.
- **پشتیبانی دوگانه (VLESS + Trojan):** تست شد و اثبات شد که در هسته دست‌نخورده، تروجان و ویلس هر دو هم‌زمان پینگ معتبر و پایدار می‌دهند.
- **دیتابیس D1:** پایگاه داده اختصاصی `super_panel_db` با شناسه مشخص و حفظ جداول کاربران و نودها.

---

## ۴. اطلس فنی: هسته نهان و سبک کدنویسی آن
- **محل و نقش:** ورکر اصلی (`_worker.js`) مبتنی بر ساختار D1 KV Store.
- **نقاط حساس (خط قرمز):** 
  - توابع هسته وب‌سوکت، توابع `connect()`، و توابع پردازش باینری استریم نباید با دستکاری‌های غیراصولی دستخوش تغییر شوند؛ زیرا منجر به پینگ صفر و قطع ارتباط تروجان/ویلس می‌شود.
- **سبک روتینگ نهان:** روت‌ها بر اساس مسیرهای داخلی و APIها هدایت می‌شوند؛ برای افزودن روت‌های مِهر باید این کار در لایه بالادستی (Middleware/Router Wrapper) انجام شود نه با دستکاری داخل هسته.

---

## ۵. اطلس فنی: هسته BPB و نودها
- **محل کد مرجع:** فایلهای `/home/reza/Downloads/bpb_decoded_core.js` و پروژه مستقل `node1.rezaai1.workers.dev`.
- **مسیرهای پیش‌فرض ترافیک در BPB:**
  - مسیر VLESS: گوش دادن روی روت `/vl?ed=2560`
  - مسیر Trojan: گوش دادن روی روت `/tr?ed=2560`
  - اعتبارسنجی تروجان: هش `sha224(password)`
- **مکانیزم حیاتی Proxy IP:** استفاده از آرایه Clean IP و آی‌پی‌های پروکسی برای مواقعی که اتصال مستقیم مسدود می‌شود (Fallback TCP).

---

## ۶. نقشه راه فعلی (در حال پیاده‌سازی)
- [x] بازگردانی پنل مِهر به نسخه سالم قبلی و اخذ بکاپ‌های مطمئن.
- [ ] راه‌اندازی و تثبیت نود سبک BPB به عنوان موتور ترافیک مستقل.
- [ ] اتصال پنل مِهر به این نود با حفظ تمام ویژگی‌های مِهر (UI، روتر هوشمند و نام‌گذاری).
- [ ] ثبت دائمی کدهای نهایی در این مستند.

---

## ۷. گنجینه تجربیات قبلی (زیرساخت و تنظیمات پیشرفته)
1. **دامنه‌ها و DNS:**
   - استفاده از ساب‌دامنه‌های DigitalPlat به عنوان ورودی‌های پایدار.
   - پیکربندی Shecan Anti-Sanction DNS روی ساختار تولید کانفیگ‌ها برای عبور مستقیم از تحریم‌ها بدون درگیر کردن ترافیک خارجی.
2. **کلاینت Karing و کانفیگ‌های نهایی:**
   - انطباق خروجی روت‌ها با استانداردهای روتینگ Karing (تفکیک Direct / Proxy بر اساس GeoIP و Geosite).
3. **تنظیمات پایگاه داده D1:**
   - استفاده از Binding ID قطعی: `22da42e5-6a5a-4eab-a1c0-31c4127a0829` برای دیتابیس `super_panel_db`.
4. **تکنیک Early Data:**
   - اجبار هدر `?ed=2560` در URL وب‌سوکت برای حفظ پینگ فوق‌سریع.

---

## ۸. جزئیات فنی ویزارد روتر هوشمند و برندینگ مِهر
1. **ویزارد روتر هوشمند (Mehr Smart Router Wizard):**
   - **هدف:** تولید خروجی‌های سینگ‌باکس، کلش، و کارینگ بر اساس انتخاب کاربر.
   - **گزینه‌های ویزارد:**
     - بای‌پس ایران (Direct برای دامنه‌ها و IPهای داخلی).
     - فعال‌سازی خودکار DNS ضدتحریم (شکن / Anti-Sanction) برای سرویس‌های تحریمی (مانند OpenAI، مایکروسافت، داکر).
     - مسدودسازی بدافزار و تبلیغات (AdBlock / Malware).
     - روتینگ اختصاصی بر اساس کلاینت کاربر (Karing، v2rayN، Sing-box).
   - **نحوه اتصال:** تزریق Rule-Setها در مسیر سابسکریپشن بدون دستکاری هسته ورکر.

2. **دستورالعمل تغییر نام و برندینگ (نهان به مِهر):**
   - جایگزینی عنوان صفحه و متادیتا: `<title>مِهر | پنل مدیریت هوشمند</title>`.
   - هدرهای پاسخ سابسکریپشن: ست کردن `Profile-Title: base64(مِهر)`.
   - پاکسازی تمام رشته‌های متنی `Nahan` / `نهان` در تمپلیت‌های HTML و جایگزینی با لوگوتایپ و نشان مِهر.
   - حفظ تمام نام متغیرهای سیستمی و جداول دیتابیس (برای جلوگیری از شکستن کدهای زیرین).

---

## ۹. نکات طلایی شبکه، کلاینت و امنیت (تجارب عملی)
1. **استاندارد هدرهای سابسکریپشن:**
   - پاسخ مسیر ساب باید حاوی `Profile-Title` (انکود Base64 از «مِهر»)، `Profile-Update-Interval: 24` و هدر `Subscription-Userinfo` باشد تا کلاینت‌ها بدون باگ آپدیت شوند.
2. **منطق هش تروجان (Trojan Auth):**
   - احراز هویت تروجان وب‌سوکت دقیقاً بر مبنای مقایسه ۵۶ بایت هدر با هش `crypto.createHash('sha224').update(password).digest('hex')` انجام می‌شود.
3. **فرمول تزریق Clean IP:**
   - فیلد آدرس اتصال = Clean IP کلودفلر
   - فیلد Host و SNI = دامنه ورکر (یا ساب‌دامنه DigitalPlat)
4. **احراز هویت پایدار Wrangler:**
   - استفاده از متغیر محیطی `CLOUDFLARE_API_TOKEN` برای بای‌پس خطاهای ورود و چالش‌های بات کلودفلر در ترمینال.

---

## ۱۰. کالبدشکافی فنی توابع هسته BPB (Core Engine Map)
1. **روت‌های وب‌سوکت ترافیک:**
   - مسیر `/vl`: هندلر `Ro` -> پارس هدر و احراز هویت UUID با `No`.
   - مسیر `/tr`: هندلر `Do` -> پارس هدر SOCKS5 و احراز هویت گذرواژه با هش `sha224` در `Io`.
2. **پایپ استریم داده (Stream Piping):**
   - خواندن Early Data کلاینت از هدر `Sec-WebSocket-Protocol` (`xo` و `Co`).
   - باز کردن سوکت اتصال TCP با `cloudflare:sockets` (`connect`).
   - جابه‌جایی جریان وب‌سوکت و سوکت کلاودفلر با `ReadableStream` و `WritableStream` (`So`).
3. **موتور نجات Fallback به Proxy IP (`ko`):**
   - در صورت عدم دریافت داده یا خطای اتصال مستقیم (مثل فیلترینگ کلاودفلر به مقصد)، فوراً اتصال جایگزین از طریق آی‌پی‌های پروکسی تصادفی بازآفرینی می‌شود.

---

## ۱۱. معماری کلان و سیستم نصب ویزارد مِهر (Master Architecture)
1. **ساختار توزیع‌شده (Distributed Architecture):**
   - **پنل مرکزی (Mehr Hub):** مبتنی بر UI نهان، دیتابیس D1، مدیریت کاربران، صدور سابسکریپشن و ویزارد روتینگ هوشمند.
   - **نودهای ترافیکی (Edge Nodes):** ورکرهای بدون UI (Headless) شامل هسته فوق‌سبک BPB، روت‌های `/vl` و `/tr`، پروکسی‌آی‌پی و بدون پردازش اضافی جهت حداکثر سرعت و پنهان‌کاری در کلودفلر.
2. **ویزارد استقرار خودکار (Automated Deploy Wizard):**
   - اتصال به کلودفلر از طریق `CLOUDFLARE_API_TOKEN`.
   - امکان انتخاب نقش ورکر در لحظه نصب: «پنل مدیریت» یا «نود فرعی ترافیک».
   - استخراج خودکار آدرس نود و ثبت آن در دیتابیس پنل مرکزی.
3. **زیرساخت گیت‌هاب و بروزرسانی:**
   - پشتیبانی از مخزن گیت‌هاب جهت واکشی آخرین آپدیت‌ها و سینک شدن پنل و نودها هم از طریق UI و هم از طریق ترمینال/ویزارد.

---

## ۱۲. مشخصات مخزن گیت‌هاب و اسکریپت‌های ویزارد موجود
- **آدرس مخزن رسمی:** `https://github.com/Reeeza2005/mehr-panel.git`
- **فایل‌های کلیدی ویزارد در ریشه:**
  - `mehr-wizard.js`: منطق اجرایی و تعاملی ویزارد در محیط Node.js.
  - `build_unified_wizard.py`: اسکریپت پایتون جهت ادغام و بیلد فایل نهایی نصاب ویزارد.
  - `make_wizard_github.py`: آماده‌سازی پکیج نصاب برای توزیع روی گیت‌هاب.

---

## ۱۳. منطق استقرار ویزارد (Master/Edge Deployment)
- **دیپلوی Master (پنل):**
  - ادغام `dashboard.html` داخل `_worker.js` به عنوان ماژول.
  - ساخت خودکار دیتابیس D1 (`super_panel_db`) و اجرای Schema.
- **دیپلوی Edge (نود فرعی):**
  - واکشی سورس `mehr-agent.js` از گیت‌هاب.
  - تزریق بایندینگ `KV` و متغیر محیطی `API_KEY` (یک رشته رندوم با پیشوند `mehr_`).
  - خروجی دادن `url` و `apiKey` برای ثبت در پنل مرکزی.

---

## ۱.۱. قانون بروزرسانی دوره‌ای مستندات (Cadence Rule)
- **قانون هر ۱۰ چت:** پس از هر ۱۰ تبادل پیام در روند پروژه، موظف هستیم وضعیت پیشرفت، تغییرات کدهای تست‌شده و نکات فنی جدید را درون همین فایل (`MEHR_MANUAL.md`) بروزرسانی و تثبیت کنیم تا هیچ جزئیاتی در طول مکالمات از دست نرود.

---

## ۱۴. وضعیت تست عملیاتی نود فرعی (Edge Node Verified)
- **وضعیت:** موفق (Verified).
- **آدرس نود فعال:** `https://mehr-node1.rezaai1.workers.dev`
- **تست‌های پاس شده:**
  1. سلامت عمومی: `200 OK`
  2. اعتبارسنجی API با `API_KEY`: موفق (`/api/status`)
  3. هندشیک وب‌سوکت: `HTTP/1.1 101 Switching Protocols` روی مسیر `/vl`
- **سورس کد:** منطبق با هسته BPB، دارای سیستم نجات Proxy IP و سازگار با فلگ `nodejs_compat`.

---

## ۱۴. وضعیت تست عملیاتی نود فرعی (Edge Node Verified)
- **وضعیت:** موفق (Verified).
- **آدرس نود فعال:** `https://mehr-node1.rezaai1.workers.dev`
- **تست‌های پاس شده:**
  1. سلامت عمومی: `200 OK`
  2. اعتبارسنجی API با `API_KEY`: موفق (`/api/status`)
  3. هندشیک وب‌سوکت: `HTTP/1.1 101 Switching Protocols` روی مسیر `/vl`
- **سورس کد:** منطبق با هسته BPB، دارای سیستم نجات Proxy IP و سازگار با فلگ `nodejs_compat`.

---

## ۱۵. ادغام کامل مستر و اج در سابسکریپشن (Master-Edge Integration Verified)
- **چرخه ثبت:** گام ۱۰ چت (Cadence Rule)
- **وضعیت اتصال نود به پنل:**
  1. **اصلاح دیتابیس D1:** ستون `status` به جدول `nodes` افزوده و نود `node_mehr_1` فعال (`active`) شد.
  2. **پچ هسته پنل (`_worker.js`):** منطق تمایز بین `Clean IP` و `Edge Node` با اضافه شدن فلگ `isNode` اعمال شد؛ برای نودهای لبه، مقادیر `sni` و `host` به دامنه خود نود (`mehr-node1.rezaai1.workers.dev`) و مسیر وب‌سوکت به `/vl` تغییر یافت.
  3. **استقرار موفق پنل:** نسخه جدید روی `https://mehr.rezaai1.workers.dev` دیپلوی شد.
  4. **کشف ساختار داده‌ها:** مشخص شد دیتابیس پنل مِهر به جای جدول قدیمی `users`، از جدول مدرن `kv_store` (کلید `sys_config`) برای مدیریت کاربران زنده (مثل کاربر `11`) استفاده می‌کند.
  5. **تست عملیاتی سابسکریپشن:** لینک اختصاصی نود فرعی با موفقیت در خروجی بیس۶۴ سابسکریپشن کلاینت‌ها (مانند کاربر ۱۱) تزریق و تأیید شد:
     `vless://<UUID>@mehr-node1.rezaai1.workers.dev:443?...path=/vl#Mehr-Mehr Edge 1`

---

## ۱۶. اعتبارسنجی عملیاتی در لایه کلاینت (Live Client Traffic Verified)
- **وضعیت:** موفق و تایید شده در شرایط واقعی فیلترینگ.
- **نتیجه تست:** در میان کانفیگ‌های خروجی سابسکریپشن، تنها کانفیگ متصل به نود لبه (`Mehr-Mehr Edge 1`) پینگ موفق و اتصال پایدار را ثبت کرد.
- **تحلیل فنی:** جداسازی کامل هاست (`mehr-node1.rezaai1.workers.dev`)، مسیر خالص `/vl` و مکانیزم نجات Proxy IP در هسته نود فرعی، تاب‌آوری شبکه را در مقایسه با روت‌های اشتراکی پنل اصلی به اثبات رساند.

---

## ۱۷. مدرن‌سازی نصاب ویزارد و حذف وابستگی به KV نودهای لبه (Stateless Wizard Alignment)
- **چرخه ثبت:** گام ۱۰ چت (Cadence Rule)
- **اقدامات انجام‌شده:**
  1. **بهینه‌سازی هسته ویزارد (`mehr-wizard.js`):** تابع `deployEdge` بازنویسی شد؛ وابستگی به ساخت KV Namespace (`MEHR_AGENT_KV`) به طور کامل حذف گردید تا فرآیند ساخت نود لبه فوق‌العاده سبک، سریع و بدون مصرف سهمیه اکانت کلودفلر انجام شود.
  2. **یکپارچه‌سازی هسته لبه (`mehr-agent.js`):** هسته مدرن و تست‌شده BPB (شامل موتور استریم سبک VLESS/Trojan، هندلینگ پروکسی IP و احراز هویت مستقل بر پایه `API_KEY`) جایگزین نسخه پیشین در پروژه و مخزن گیت‌هاب شد.
  3. **تولید مجدد پکیج نصاب:** فایل یکپارچه نصاب از طریق `build_unified_wizard.py` بازسازی و روی مخزن گیت‌هاب `origin/main` پوش گردید.
  4. **برنامه‌ریزی تحلیل پنل نهان:** کالبدشکافی منطق تخصیص Clean IP/Proxy IP، استتار وب (Camouflage fallback) و پروتکل تبادل کلید نودها در سورس نهان برای ادغام بی‌نقص در نسخه آینده ویزارد.

---

## ۱۸. عیب‌یابی ارتباط کلاستر و همگام‌سازی کلیدهای احراز هویت نود لبه
- **چرخه ثبت:** گام ۱۰ چت (Cadence Rule)
- **شرح مسئله:**
  - نود تستی (`node1.rezaai1.workers.dev`) با مکانیزم استتار فعال شد و صفحه اوبونتو را بدون خطا بازگرداند.
  - اما در زمان استعلام وضعیت از پنل اصلی یا تست با کلید پیش‌فرض، نود پاسخ `401 Unauthorized` برگرداند و وضعیت آن آفلاین اعلام گردید.
- **ریشه‌یابی فنی:**
  1. تابع `deployEdgeNode` در نصاب ویزارد یک کلید تصادفی موقت (`mehr_...`) تولید کرده و به `API_KEY` متصل نموده بود، اما این کلید در خروجی پایانی چاپ نشد.
  2. پنل مستر درخواست ارزیابی را با کلید پیش‌فرض کلاستر (`mehr_cluster_key_2026`) ارسال می‌کرد که به دلیل عدم تطابق، از سمت گره رد می‌شد.
- **برنامه اقدام:**
  - هماهنگ‌سازی ویزارد جهت تنظیم کلید معتبر کلاستر روی متغیر `API_KEY` ورکر لبه.
  - چاپ صریح `Node URL` و `API Key` در انتهای فرآیند ساخت نود توسط ویزارد جهت ورود دقیق در فرم پنل.

---

## ۱۹. یکپارچه‌سازی تابع استقرار نود لبه در ویزارد
- **تغییرات:**
  - حذف بایندینگ اضافه KV (`MEHR_AGENT_KV`) از ساختار متادیتای نود فرعی.
  - تنظیم مقدار پیش‌فرض `API_KEY` روی کلید استاندارد کلاستر (`mehr_cluster_key_2026`).
  - تزریق خودکار آخرین نگارش مجهز به استتار `mehr-agent.js` درون کالبد نصاب ویزارد.

---

## ۲۰. اصلاح انطباق اسکیما جدول نودها در ورکر اصلی
- **شرح مسئله:**
  - جدول `nodes` در D1 ستون‌های `(id, name, url, api_key, created_at, status)` را داراست، در حالی که کنترلر `_worker.js` فیلدهای فرضی `address` و `last_seen` را هدف قرار داده بود که به خطای `SQLITE_ERROR: table nodes has no column named address` منجر می‌شد.
- **اقدام:**
  - انطباق کوئری‌های `INSERT` و `SELECT` با شناسه فیلد `url` و `created_at`.
  - نگاشت مجازی `address = url` جهت سازگاری کامل فرانت‌اند.

---

## ۲۱. رفع مغایرت منطق محاسبه وضعیت آنلاین بودن نودها
- **علت:** فیلد `last_seen` در اسکیما پایگاه داده تعریف نشده بود؛ در نتیجه کوئری مستر به دلیل ارزیابی زمان بر مبنای مقدار ناموجود، همواره `is_online: false` بازمی‌گرداند.
- **اصلاحات:**
  - تطبیق پرچم `is_online` مستقیماً با وضعیت فعال گره (`status === "active"`).
  - تزریق مشخصه `address` معادل `url` جهت انطباق بدون نقص با کامپوننت‌های فرانت‌اند داشبورد.

---

## ۲۲. عیب‌یابی پینگ مرورگر نودها و پشتیبانی CORS در اجنت
- **شرح مسئله:**
  - نود در رابط داشبورد در وضعیت «⏳ در حال تست...» معلق می‌ماند.
  - فرانت‌اند به مسیر `/api/stats` درخواست می‌داد در حالی که نود اندپوینت `/api/status` داشت.
  - درخواست فرانت‌اند کراس-اورجین (از دامین مستر به دامین ورکر نود) به دلیل عدم وجود هدرهای CORS مسدود می‌شد.
- **اقدام:**
  - تطبیق مسیر استعلام فرانت‌اند داشبورد به `/api/status` (با فال‌بک به `/api/stats`).
  - افزودن هدرهای کامل CORS (`Access-Control-Allow-Origin: *`) در پاسخ‌های اندپوینت وضعیت نود لبه.

---

## ۲۳. پیاده‌سازی ماتریس ضرب کانفیگ‌ها و نودهای اطلاعاتی تروجان (طرح نهان)
- **منطق ماتریس ضرب:**
  - به ازای هر نود (مستر یا اجنت فرعی):
    - در صورت تعریف آی‌پی‌های تمیز: آدرس مستقیم دامنه حذف شده و تنها آی‌پی‌های تمیز به عنوان آدرس اتصال قرار می‌گیرند (با SNI و Host دامنه مربوط به همان نود).
    - ترکیب کامل با پروکسی آی‌پی‌ها (`proxyip.txt` یا مقادیر ست‌شده).
    - تولید کانفیگ‌های هر دو پروتکل `VLESS` و `Trojan`.
- **نودهای مجازی اینفورمیشن (Trojan Info Nodes):**
  - کانفیگ اول: نمایش میزان مصرف حجم (`📊 ترافیک`).
  - کانفیگ دوم: نمایش تاریخ انقضا و وضعیت اکانت (`⏳ انقضا`).
